<main>
  <div class="mast container">
    <header class="mast__header">
      <div class="">
          <h1 class="mast__title js-spanize d-flex justify-content-center py-5">Welcome !</h1>
          <p class="mast__text js-spanize py-3">There is only one feature in this version, for now.</p>
      </div>
      <div class="main_links py-3 px-5">
        <%= link_to "Curriculum FR", sheet_path(Sheet.where(language: 'french').first.id) %>
        <%= link_to "Curriculum EN", sheet_path(Sheet.where(language: 'english').first.id) %>
        <%# <%= link_to "Playground", "playground" %>
      </div>
    </header>
  </div>
  <p class="mast__text js-spanize" id="version">Version ßeta</p>
</main>
